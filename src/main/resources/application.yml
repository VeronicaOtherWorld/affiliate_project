spring:
  datasource:
    url: jdbc:mysql://localhost:3306/affiliate_project
    username: root
    password: new_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect

  security:
    user:
      name: admin
      password: admin

server:
  port: 8080  # running in the port 8080

